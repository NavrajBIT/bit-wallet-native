import { View, Text } from "react-native";

const Tokencontainer = () => (
  <View>
    <Text style={{ fontSize: 30, color: "white" }}>Assets shown here...</Text>
  </View>
);

export default Tokencontainer;
